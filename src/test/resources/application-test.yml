spring:
  config:
    activate:
      on-profile: test

  database:
    name: ${INTERSHOP_TEST_DB_NAME}
    username: ${INTERSHOP_DB_USERNAME}
    password: ${INTERSHOP_DB_PASSWORD}

  liquibase:
    contexts: 'test'